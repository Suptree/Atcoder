#include <bits/stdc++.h>

#define repo(i,n) for (int i = 0; i < n; i++)
using namespace std;
using ll = long long;

int main(){
    
    string s = "abcdefghij"
    

    int N;
    cin >> N;
    int count = 0;
    for(int i = 0; i < N; i++){

        cout << s.at(i) << endl;
    }
    return 0;
}

